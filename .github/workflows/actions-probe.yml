name: Actions Probe

on:
  pull_request:
    branches: [main]

permissions: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Probe runner startup
        run: echo "GitHub Actions runner started"
