name: Central metadata routing

on:
  issues:
    types: [labeled, unlabeled]
  schedule:
    - cron: '47 * * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  metadata:
    uses: Avkroken/.github/.github/workflows/metadata-orchestration.yml@980d67fa6e18a4800dd90fdf5cd8f463b236de6c
