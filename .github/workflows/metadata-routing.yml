name: Central metadata routing

on:
  issues:
    types: [labeled, unlabeled]
  schedule:
    - cron: '47 * * * *'
  workflow_dispatch:

permissions: {}

jobs:
  metadata:
    permissions:
      issues: write
      pull-requests: write
    uses: Avkroken/.github/.github/workflows/metadata-orchestration.yml@1c5f92d60bfae9151d889e0db1f5e1ecec6a93e2
