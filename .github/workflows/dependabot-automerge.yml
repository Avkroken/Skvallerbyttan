name: Dependabot queue events

on:
  schedule:
    - cron: '41 * * * *'
  workflow_dispatch:

permissions: {}

jobs:
  queue:
    uses: Avkroken/.github/.github/workflows/dependabot-automerge.yml@abfaf7aae8a69f1dfa6bc7e01440496edb59def6
    secrets:
      app-private-key: ${{ secrets.GAMNACKEN_PRIVATE_KEY }}
