name: Dependabot queue events

on:
  schedule:
    - cron: '41 * * * *'
  workflow_dispatch:

permissions: {}

jobs:
  queue:
    uses: Avkroken/.github/.github/workflows/dependabot-automerge.yml@4c42428e6e4faf52983b398aa028ca1589380bb6
    secrets:
      app-private-key: ${{ secrets.GAMNACKEN_PRIVATE_KEY }}
